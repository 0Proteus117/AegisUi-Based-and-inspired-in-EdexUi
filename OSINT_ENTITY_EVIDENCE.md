# Entity Evidence

Only an explicit `ADD TO CASE` action promotes a selected entity snapshot. The
existing Evidence Preview performs redaction before the canonical SHA-256 hash
is created. Persisted data is limited to selected entity attributes,
relationships, provenance, contradictions, confidence and analyst note. Raw
provider payloads, graph coordinates and hidden runtime state are excluded.
