#!/usr/bin/env node
"use strict";

// Sanitized renderer/layout validation. It injects only synthetic in-memory
// package/response data: no IPC, Ollama, provider, filesystem or STUD store is
// contacted while creating release evidence.
const fs = require("fs");
const port = Number(process.argv[2] || 9269);
const screenshotPath = process.argv[3] || "";
const width = Number(process.argv[4] || 1440);
const height = Number(process.argv[5] || 900);
const scale = Number(process.argv[6] || 2);
const appearance = String(process.argv[7] || "dark").toLowerCase();
const variant = String(process.argv[8] || "grounded").toLowerCase();
const delay = ms => new Promise(resolve => setTimeout(resolve, ms));
async function connect() { const pages = await (await fetch(`http://127.0.0.1:${port}/json/list`)).json(); const page = pages.find(item => item.type === "page"); if (!page) throw new Error("No Electron renderer page found."); const socket = new WebSocket(page.webSocketDebuggerUrl); socket.seq = 0; socket.pending = new Map(); await new Promise((resolve, reject) => { socket.addEventListener("open", resolve, {once: true}); socket.addEventListener("error", reject, {once: true}); }); socket.addEventListener("message", event => { const msg = JSON.parse(event.data), pending = socket.pending.get(msg.id); if (!pending) return; socket.pending.delete(msg.id); if (msg.error || msg.result?.exceptionDetails) return pending.reject(new Error(JSON.stringify(msg.error || msg.result.exceptionDetails))); pending.resolve(pending.raw ? msg.result : msg.result?.result?.value); }); return socket; }
function command(socket, method, params = {}) { const id = ++socket.seq; return new Promise((resolve, reject) => { socket.pending.set(id, {resolve, reject, raw: true}); socket.send(JSON.stringify({id, method, params})); }); }
function evaluate(socket, expression) { const id = ++socket.seq; return new Promise((resolve, reject) => { socket.pending.set(id, {resolve, reject, raw: false}); socket.send(JSON.stringify({id, method: "Runtime.evaluate", params: {expression, returnByValue: true, awaitPromise: true}})); }); }
function fixture() { return `(async()=>{const m=window.workspaceManager;m.activate('student',false);await new Promise(r=>setTimeout(r,250));const cc=m.studCommandCenter,now='2026-08-13T12:00:00.000Z';const course={entityType:'COURSE',id:'stud_course_ai',title:'Synthetic Systems and Evidence Course',code:'SYN-100',status:'ACTIVE'};const assignment={entityType:'ASSIGNMENT',id:'stud_assignment_ai',courseId:course.id,title:'Synthetic long assignment title for a bounded local academic assistant validation',description:'Explain the synthetic local stability and force evidence without external sources.'};const note={entityType:'NOTE',id:'stud_note_ai',courseId:course.id,assignmentId:assignment.id,title:'Synthetic local lecture note with long safe content',content:'Force equals mass multiplied by acceleration. Stability is discussed in local lecture material.'};const pkg={id:'stud_context_package_ai',title:'Synthetic reviewed local Context Package',rootType:'ASSIGNMENT',rootId:assignment.id,status:'TRUNCATED',createdAt:now,omitted:[{reason:'PACKAGE_TEXT_LIMIT'}],snapshot:{root:{entityType:'ASSIGNMENT',entityId:assignment.id},candidates:[{entityType:'ASSIGNMENT',entityId:assignment.id,title:assignment.title,relationship:'DIRECT',reasons:['Selected academic context root']},{entityType:'NOTE',entityId:note.id,title:note.title,relationship:'DIRECT',reasons:['Explicit HAS NOTE relationship']}],chunks:[],fragments:[{entityType:'NOTE',entityId:note.id,title:note.title,kind:'NOTE_TEXT',content:note.content}],policy:{offline:true,selectedTextOnly:true}}};cc.state.schema={version:10};cc.state.error=null;cc.state.courses=[course];cc.state.assignments=[assignment];cc.academicAssistant.state={...cc.academicAssistant.state,choices:{ASSIGNMENT:[assignment],COURSE:[course],RESEARCH_PAPER:[],ACADEMIC_DOCUMENT:[],NOTE:[note],REVISION_ITEM:[]},rootType:'ASSIGNMENT',rootId:assignment.id,packages:[pkg],package:pkg,engine:{status:'READY',model:'llama3.2:3b'},mode:'EXPLAIN',question:'What does the bounded local source state about force and stability?',busy:false,error:null,response:{responseId:'stud_academic_ai_response_visual',status:'PARTIAL',model:'llama3.2:3b',generatedAt:now,answer:'The supplied local material states that force equals mass multiplied by acceleration. It also identifies stability as a topic in the local lecture context. The package is truncated, so this response does not claim complete coverage.',claims:[{text:'The source states the force relation.',sourceRefs:['S-F1']},{text:'Stability is present in the local context.',sourceRefs:['S-F1']}],limitations:['Context Package is truncated; omitted material was not available to the model.','Local source material does not establish broader conclusions.'],sourceTrace:[{id:'S-F1',entityType:'NOTE',entityId:note.id,title:note.title,kind:'NOTE_TEXT',pageStart:null}],retrieval:{strategy:'FTS5_RESTRICTED_TO_CONTEXT_PACKAGE_THEN_LEXICAL_RANK',selectedSources:1}},candidates:[{candidateId:'visual_1',title:'Review the local force relation',prompt:'What relation does the local source state?',answer:'Force equals mass multiplied by acceleration.',sourceRefs:['S-F1']}],saveResponseId:null};cc.setActiveView('AI');return true})()`; }
async function main() { const socket = await connect(); try { await command(socket, "Emulation.setDeviceMetricsOverride", {width, height, deviceScaleFactor: scale, mobile: false}); const system = appearance.startsWith("system-") ? appearance.slice(7) : null; if (system) await command(socket, "Emulation.setEmulatedMedia", {features: [{name: "prefers-color-scheme", value: system}]}); await evaluate(socket, `document.documentElement.dataset.aegisAppearance=${JSON.stringify(system || appearance)};true`); await evaluate(socket, fixture()); if (variant === "unavailable") await evaluate(socket, `(()=>{const a=window.workspaceManager.studCommandCenter.academicAssistant;a.state.engine={status:'OFFLINE',model:null,reason:'Synthetic local Ollama unavailable validation state.'};a.state.response=null;a.state.candidates=[];window.workspaceManager.studCommandCenter.render()})()`); if (variant === "package") await evaluate(socket, `(()=>{const a=window.workspaceManager.studCommandCenter.academicAssistant;a.state.response=null;a.state.candidates=[];a.state.error=null;window.workspaceManager.studCommandCenter.render()})()`); if (variant === "long") await evaluate(socket, `(()=>{const a=window.workspaceManager.studCommandCenter.academicAssistant;a.state.question='Explain the bounded local material with a deliberately long synthetic academic question that validates wrapping, source-trace reachability and the explicit persistence boundary across compact AegisUi desktop dimensions.';a.state.response.answer='The supplied local material is intentionally bounded. It states the force relation and identifies stability as a local topic. This extended synthetic response verifies that long grounded content remains inside the analytical surface, preserves readable source references, and does not claim knowledge beyond the selected Context Package.';a.state.response.limitations.push('Long synthetic validation content remains a limitation statement, not a claim of broader coverage.');window.workspaceManager.studCommandCenter.render()})()`); await delay(450); const report = await evaluate(socket, `(()=>{const root=document.querySelector('[data-stud-main]'),rect=e=>e.getBoundingClientRect(),panels=[...root.querySelectorAll('.workspace-panel')],controls=[...root.querySelectorAll('button,input,textarea,select')].filter(e=>!e.disabled),escaped=controls.filter(e=>{const p=e.closest('.workspace-panel')||root,a=rect(e),b=rect(p);return a.left<b.left-3||a.right>b.right+3||a.top<b.top-3||a.bottom>b.bottom+3}),overlaps=[];for(let i=0;i<panels.length;i+=1)for(let j=i+1;j<panels.length;j+=1){const a=rect(panels[i]),b=rect(panels[j]);if(a.right>b.left+5&&b.right>a.left+5&&a.bottom>b.top+5&&b.bottom>a.top+5)overlaps.push([i,j]);}return{variant:${JSON.stringify(variant)},available:!!root,assistant:!!root.querySelector('.stud-ai-shell'),trace:!!root.querySelector('.stud-ai-trace'),limits:!!root.querySelector('.stud-ai-limitations'),escaped:escaped.length,overflow:root.scrollWidth>root.clientWidth+4,overlaps:overlaps.length,panels:panels.length}})()`); const responseOptional=['unavailable','package'].includes(variant); const valid=report.available&&report.assistant&&(responseOptional||(report.trace&&report.limits))&&!report.escaped&&!report.overflow&&!report.overlaps; console.log(`STUD_PHASE10_LIVE_LAYOUT: ${valid?'OK':'FAIL'} ${JSON.stringify(report)}`); if(screenshotPath){const capture=await command(socket,'Page.captureScreenshot',{format:'png'});fs.writeFileSync(screenshotPath,Buffer.from(capture.data,'base64'));console.log(`STUD_PHASE10_SCREENSHOT: ${screenshotPath}`);} if(!valid)process.exitCode=1;}finally{socket.close();} }
main().catch(error=>{console.error(`STUD_PHASE10_LIVE_LAYOUT: FAIL ${error.message}`);process.exitCode=1;});
