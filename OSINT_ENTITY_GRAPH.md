# Entity Graph

The graph is a bounded visual review surface: nodes are entities and edges are
evidence-backed relationships. It has a hard limit of 50 nodes and 100 edges,
supports type/status filtering and uses a small deterministic radial layout.
Selection is available through an accessible node list; the graph never causes
automatic provider queries or graph expansion.
